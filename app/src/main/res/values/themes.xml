<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicWithFriends" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="android:windowLightStatusBar">@color/black</item>
        <item name="android:navigationBarColor">@color/blue</item>
        <item name="android:windowActivityTransitions">true</item>
    </style>

    <style name="ThemeOverlay.AppTheme.TextInputEditText.Outlined" parent="" >
        <item name="colorPrimary">@color/white</item>
        <item name="android:colorControlActivated">@color/black</item>
    </style>

    <style name="AnimationContextMenu">
        <item name="android:windowEnterAnimation">@anim/slide_in_context_menu</item>
        <item name="android:windowExitAnimation">@anim/slide_out_context_menu</item>
    </style>

    <style name="AnimationDialog">
        <item name="android:windowEnterAnimation">@anim/slide_in_dialog</item>
        <item name="android:windowExitAnimation">@anim/slide_out_dialog</item>
    </style>

    <style name="AnimationClientDialogInfo">
        <item name="android:windowEnterAnimation">@anim/slide_in_dialog</item>
    </style>

</resources>